

// const getLargest = arr => {
//     // sort the array in ascending order
//     arr.sort((a, b) => {
//         return a - b
//     });
//     // pop off the last(largest) value
//     return arr.pop()
//     }
// console.log(getLargest([2, 2, 1]));

const balancedXO = str => {
    //set up a loop through the string
    //have a counterX and counterO variable increment as the looped index finds them. 
    //compare the counter values, condititional to determine if they're equal.  
}